# Tests (Playwright)

Tests E2E automatisés sur la base des critères d'acceptation des User Stories (`/specs/user-stories`).

```bash
npx playwright test
```

Résultats et captures d'écran/vidéos dans ce dossier (`.gitignore` exclut les rapports volumineux).
